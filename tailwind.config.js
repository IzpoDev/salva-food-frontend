/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.html", // Busca en todos los archivos HTML dentro de src
    "./src/**/*.ts",   // Busca en todos los archivos TypeScript dentro de src
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}

